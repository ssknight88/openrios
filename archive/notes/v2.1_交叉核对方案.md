# v2.1 交叉核对方案（草案，批准后执行）

**对象**：三方交叉——`Module_modified.md`（参照，排除 FU 段；四处已知过时段落豁免）
× `Module.md`（索引）× `module_spec_v2/` 23 份（正文）。
**执行者**：助理（同上下文会话），先通读全部成品再逐维度核对。

## 一、命名守恒

1. 旧名全库 grep 归零：`INT_DST_REG`/`FP_DST_REG`/`p1_check_resolve`/`p1_dsp`/
   `p1_isq_group_select`/`isq_group_select`（已并入 dispatch_logic）/`PCFile`/`csr_file`/
   `csr_stage 在 SCB`/`serial_tracker`（正式名称为 SerialInstructionTracker，信号 `serial_*` 前缀不在此列）。
   **豁免清单**（仅此四处允许出现旧名）：system_instruction_handler 引言"原 csr_file"、
   dispatch_logic 引言"原 isq_group_select"、Module.md §6.4 与 §9 历史注记、
   `v2.1_变更计划.md` 本身
2. 新名拼写一致：`dispatch_logic`（文件/正文）vs "dispatch logic"（参照显示名）、
   `INT/FP_tag_mapping`、`dependency_check`、`PC_File`、
   `system_instruction_handler`——大小写/下划线全库统一
3. [[链接]] 全量解析：每个 wiki 链接指向存在的文件；带章节锚的引用（如"④ 第三步"）
   所指章节在目标文档中确实存在
4. 信号名与模块名解耦处核对：`Buffer_head`/`Buffer_tail` 沿用注记仍在（Buffer ⑥、SCB Static Info、
   dependency_check ⑥）；`serial_set` 产生/载荷/消费三处

## 二、三方结构对照

1. 参照每个 submodule ↔ Module.md 条目 ↔ spec 文件的**三方映射表**逐行核对，
   含层级：scoreboard 四子模块、dispatch_logic（选组已并入，含两 mux 拆解行；
   `select_payload` 成为其对 p1_ISQ_input_mux 的唯一新增出口、
   `slot0_fire_candidate` 内部化后无跨界消费者）、
   ISQ 拆解行（FU_input_mux）、库外段（FU 全家 + FU_group_arbiter 指针）
2. 每处差异归类判定：**参照过时**（限四处豁免：肥 Buffer、CompletionSCB 的
   csr_clear/is_serial/CSR 内容、SerialInstructionTracker 机制、"外围 CSR 寄存器堆"）/
   **本地裁定覆盖** / **真偏差**（报 BLOCKER 或 DECISION）
3. 计数守恒：22 份文件 = 20 库内 + 2 库外契约宿主（p3_arbiter_G0/G1），
   分布跨主目录与 checked_file/ 两处；Module.md 对 22 份全覆盖、无多余条目

## 三、B 迁移的信号守恒（重点）

1. **lane 0 CSR 四字段**：新消费者 SIH 的 capture 登记完整且三方一致——
   p3_arbiter_G0 ⑤ 注记、SIH ⑥ 写回侧、SCB ⑥"不进本模块"
2. **commit lanes**：五个消费者（INT/FP ARF、INT/FP_tag_mapping、dependency_check、
   SerialInstructionTracker、SIH）各自 ⑥ 的字段需求 vs SCB ⑥ 的供给逐一配对
3. **commit_count**：SIH 的 minstret 输入登记闭合
4. **global_flush_late 消费者全表**：SIH（只清 csr_stage）、SCB（只作 writeback guard，
   指针落位由 flush_valid 自产）、IB/ISQ×4/tag_mapping×2/SerialInstructionTracker（原样）
5. **arch_csr_write 事件消亡**后全库无悬空引用；**trap_state_write** 对端改指后
   五字段（valid/kind/epc/cause/tval）两端一致
6. **SCB 削减后模板自洽**：② 的每个 event 在 ③ 有判据、④ 的每个信号在 ⑥ 有登记、
   ⑤ 字段三角色与 ③/④ 用法一致——重点查 capture/apply 删除后无孤儿引用

## 四、SIH 语义保真

1. SIH vs 原 csr_file **逐段 diff**：trap_entry / mret_update（含 MPP 行）、计数器、
   WARL 忽略、mip 直驱、csr_fu 契约、派生组合三式、软件读口——只重组不改义，
   丢句/改义逐条列出
2. csr_stage 三判据（capture/apply/flush）与迁移前 SCB 版本**逐字段等价**；
   "capture 与 apply 至少隔一拍"论证保留
3. 待补挂点完整性：fcsr/FS、U 态、五条缓行指令、FENCE.I 技巧、SFENCE.VMA 全部有落点，
   且与 IB ⑤ 待补注记互指一致

## 五、历史裁定回归抽检

逐项确认未被本轮回退或误伤：瘦 Buffer（用户手订版为准）、busy 保留 +"映射有效位"新描述、
tracker tag 自清、FU flush 契约与完成请求寄存契约（p3_arbiter_G0 ④ 权威 + G1 引用）、
"flush 恒清全窗"前提（tag_mapping×2 ③）、valid 投影、FU 不入库、
判定链六/四分支与七行 flush_tag 表保级、双 FP 缩 1 依据链（FP_ARF/FP_tag_mapping → SCB ④ 第三步）。

## 六、输出与收敛

- 分级 **BLOCKER / DECISION / MINOR / PASS**，每条附文件与行号
- 结论一行：可收货 / 修正后收货 / 有 BLOCKER
- 发现即修即复验，直至：旧名 grep 表、链接解析表、三方映射表**三表全绿**
